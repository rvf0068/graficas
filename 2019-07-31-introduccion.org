#+title: Gráficas simples
#+author: 
#+date: 2019-07-31 17:00 -0500

#+setupfile: options.org
#+beamer: \languagepath{spanish}

* Definiciones

** Gráficas simples

*** Gráfica, vértices, aristas 				       :B_definition:
    :PROPERTIES:
    :BEAMER_env: definition
    :END:
    Una *gráfica simple* es un par ordenado \(G=(V(G),E(G))\) tal que:
    - \(V(G)\) es un conjunto, cuyos elementos se llaman *vértices*,
    - \(E(G)\) es un conjunto cuyos elementos son subconjuntos de
      \(V(G)\) de cardinalidad 2. Los elementos de \(E(G)\) se llaman *aristas*.
    - Si denotamos el conjunto de parejas de elementos de un conjunto
      \(X\) como \(\binom{X}{2}\), tenemos que \(E(G)\subseteq
      \binom{V(G)}{2}\).

*** Ejemplo
    Definamos \(G\) como \(G=(V(G),E(G))\) por
    medio de \(V(G)=\{1,2,3\}\), \(E(G)=\bigl\{\{1,2\},\{2,3\}\bigr\}\).

** Otros ejemplos:
   - \(V(G)=\mathbb{N}\), \(E(G)=\{\{x,y\}\mid |x-y|\geq 5\}\).
   - \(V(G)=\{1,2,3,\ldots,20\}\), \(E(G)=\{\{x,y\}\mid |x-y|\text{ es
     primo}\}\).
   - \(V(G)=\{x\mid x\text{ es alumno de la UAEH}\}\),
     \(E(G)=\{\{x,y\}\mid x,y\text{ son amigos}\}\).
   - \(V(G)\) es el conjunto de las ciudades del mundo, \(E(G)\) es el
     conjunto de las parejas de ciudades entre las cuales existe un vuelo
     comercial directo.
   - \(V(G)\) es el conjunto de las 28 fichas de dominó, \(E(G)\) es
     el conjunto de parejas de fichas que se pueden juntar de acuerdo
     a las reglas.
   - Dada una gráfica \(G\), se define una gráfica \(L(G)\) como:
     - \(V(L(G))=E(G)\),
     - \(E(L(G))=\{\{e_{1},e_{2}\}\mid e_{1}\ne e_{2}, e_{1}\cap e_{2}\ne\emptyset\}\).

** Adyacencia

*** Vértices adyacentes
    Si \(\{v,w\}\in E(G)\), decimos que \(v,w\) son *adyacentes*, y
    escribimos \(v\sim w\).

*** Grado
    Si \(v\in V\), el *grado* de \(v\) es la cantidad de vértices
    adyacentes a \(v\).

** Dibujo de una gráfica
   La gráfica dada por: \(V(G)=\{1,2,3\}\),
   \(E(G)=\bigl\{\{1,2\},\{2,3\}\bigr\}\), se puede representar con el
   siguente dibujo: 
   
   #+name: grafica1
   #+header: :file (by-backend (latex "grafica1.tikz") (beamer "grafica1.tikz") (t "grafica1.png"))
   #+begin_src latex :results raw file
\begin{tikzpicture}
  \graph[branch right=2cm, nodes={circle, draw, inner sep=2pt}] { subgraph P_n [n=3] };
\end{tikzpicture}
   #+end_src
   
   #+attr_html: :width 400 :alt grafica1 :align center
   #+attr_latex: :float t :width 7cm
   #+RESULTS[292ddbbf08d69178ecdd13a3bc53c975bb61ff9f]: grafica1
   [[file:grafica1.png]]

   Nota que el dibujo no es único, por ejemplo, no importa el tamaño
   de las aristas.

** Multigráficas, lazos, digráficas

   - En una gráfica simple, las aristas unen vértices distintos, y
     entre un par de vértices distintos puede haber a lo más una
     arista.
   - Una *multigráfica* es parecida a una gráfica, pero además se
     permiten *aristas múltiples* y *lazos*.
   - En una *digráfica*, las aristas son pares ordenados \((u,v)\). En
     este caso, las aristas se suelen llamar *flechas*.

** Orden y tamaño

   - La cantidad de vértices de una gráfica (finita) \(G\) se llama el
     *orden* y se denota con \(|G|\).
   - La cantidad de aristas de una gráfica \(G\) se llama el *tamaño*
     de la gráfica y se denota con \(\epsilon(G)\). Nótese que
     \(\epsilon(G)\leq\binom{|G|}{2}\).

** Gráfica completa y gráfica vacía

   - La *gráfica completa* \(K_{n}\) es la gráfica simple donde
     \(V(K_{n})=\{1,2,\ldots, n\}\) y \(E(G)=\binom{V(G)}{2}\).
   - La *gráfica vacía* \(\overline{K_{n}}\) es la gráfica con
     \(V(\overline{K_{n}})=V(K_{n})\) y
     \(E(\overline{K_{n}})=\emptyset\).

** Complemento

   - Dada una gráfica \(G\), su *complemento* es la gráfica
     \(\overline{G}\) tal que \(V(\overline{G})=V(G)\) y
     \(E(\overline{G})=\binom{V(G)}{2}\setminus E(G)\).

   - Por ejemplo:

      #+name: complemento
      #+header: :file (by-backend (latex "complemento.tikz") (beamer "complemento.tikz") (t "complemento.png"))
       #+begin_src latex :results raw file
\begin{tikzpicture}
  \SetVertexNoLabel
  \grCycle[RA=2, rotation=45]{4}
  \AssignVertexLabel{a}{1,2,3,4}
  \grEmptyCycle[prefix=b, RA=2, rotation=45,x=7]{4}
  \AssignVertexLabel{b}{1,2,3,4}
  \Edge(b1)(b3)
  \Edge(b0)(b2)
  \draw (0,-2) node [fill=orange!80!white,below]{\(G\)};
  \draw (7,-2) node [fill=orange!80!white,below]{\(\overline{G}\)};
\end{tikzpicture}
       #+end_src

      #+attr_html: :width 400 :alt complemento :align center
      #+attr_latex: :float t :width 7cm
      #+RESULTS[b0ecf844150e6b4c067330362180b04f2ca9c8db]: complemento
      [[file:complemento.png]]

** Gráficas autocomplementarias

   - Hay gráficas tales que, salvo renombrar los vértices, se "ven
     igual" que su complemento. Tales gráficas se llaman
     *autocomplementarias*. Por ejemplo:
#+name: autocomplementaria
#+header: :file (by-backend (latex "autocomplementaria.tikz") (beamer "autocomplementaria.tikz") (t "autocomplementaria.png"))
#+begin_src latex :results raw file
\begin{tikzpicture}
  \SetVertexNoLabel
  \grCycle[RA=2, rotation=90]{5}
  \AssignVertexLabel{a}{1,2,3,4,5}
  \grEmptyCycle[prefix=b, RA=2, rotation=90,x=7]{5}
  \AssignVertexLabel{b}{1,2,3,4,5}
  \Edge(b0)(b2)
  \Edge(b2)(b4)
  \Edge(b4)(b1)
  \Edge(b1)(b3)
  \Edge(b3)(b0)
  \draw (0,-2) node [fill=orange!80!white,below]{\(G\)};
  \draw (7,-2) node [fill=orange!80!white,below]{\(\overline{G}\)};
\end{tikzpicture}
#+end_src

#+attr_html: :width 400 :alt autocomplementaria :align center
#+attr_latex: :float t :width 7cm
#+RESULTS[6af304710ce499900a150dc7e296d75c2a287682]: autocomplementaria
[[file:autocomplementaria.png]]

   - La correspondencia entre los vértices está dada por \(1\mapsto
     1\), \(2\mapsto 3\), \(3\mapsto 5\), \(4\mapsto 2\) y \(5\mapsto
     4\).

** Isomorfismo de gráficas

   - Se dice que dos gráficas \(G_{1}\), \(G_{2}\) son *isomorfas* si
     existe una correspondencia biyectiva \(f\colon V(G_{1})\to
     V(G_{2})\) de tal manera que \(\{v_{1},v_{2}\}\in E(G_{1})\) si y
     solo si \(\{f(v_{1}),f(v_{2})\}\in E(G_{2})\). Decimos entonces
     que \(f\) es un *isomorfismo* y escribimos \(G_{1}\cong G_{2}\)
   - Con este concepto, entonces se puede definir una gráfica
     autocomplementaria como una gráfica que es isomorfa a su
     complemento.

** Teorema de autocomplementaridad

   - *Teorema.* Si \(G\) es autocomplementaria, entonces \(|G|\equiv
     0\text{ o }1\pmod{4}\).
     - /Demostración./ Si \(G\cong \overline{G}\), entonces
       \(\epsilon(G)=\epsilon(\overline{G})\). Como
       \(E(\overline{G})=\binom{V(G)}{2}\setminus E(G)\), entonces
       \(\epsilon(\overline{G})=\binom{|G|}{2}-\epsilon(G)\), por lo
       que:
       \begin{equation*}
       \epsilon(G)=\frac{1}{2}\binom{|G|}{2}=\frac{1}{4}|G|(|G|-1).
       \end{equation*}
     - Como \(|G|\) y \(|G|-1\) son enteros consecutivos, uno de ellos
       es impar. Se deduce que no puede pasar que \(2\) divida a
       \(|G|\) y \(2\) divida a \(|G|-1\), por lo que \(4\) divide a
       \(|G|\) o \(4\) divide a \(|G|-1\), como queríamos
       demostrar. \(\square\)

* COMMENT Local Variables

# Local Variables:
# org-confirm-babel-evaluate: nil
# org-latex-prefer-user-labels: t
# End:
